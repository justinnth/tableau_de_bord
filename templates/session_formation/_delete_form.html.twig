<form method="post" action="{{ path('session_formation_delete', {'id': session_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_formation.id) }}">
    <button class="btn">Delete</button>
</form>
